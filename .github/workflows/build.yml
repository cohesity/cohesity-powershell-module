name: Build
on: [push]
jobs:
  Check-Path:
    runs-on: windows-latest
    steps:
      - name: Testing a plain message
        shell: powershell
        run: write-host "Check for current path $pwd"
